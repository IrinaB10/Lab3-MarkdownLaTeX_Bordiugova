# Блоки кода и цитаты

## Inline-код

Команда `git status` показывает состояние репозитория.

## Блок кода на C# с подсветкой синтаксиса

```csharp
int a = 5;
int b = 10;
int sum = a + b;
Console.WriteLine($"Сумма: {sum}");